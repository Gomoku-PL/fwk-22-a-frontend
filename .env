# Frontend .env for local dev
# Use the Vite proxy (see vite.config.js) to reach the backend
VITE_BASE_URL=/api/games

# Optional: enable HTTPS dev server for Vite (used by npm run dev:https)
# Update the paths if you place certs elsewhere
VITE_DEV_SSL_KEY=../dev-setup/certs/localhost-key.pem
VITE_DEV_SSL_CERT=../dev-setup/certs/localhost.pem